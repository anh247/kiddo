<div class="product-item style-01 {{ classes }}">
    <div class="product-inner animate--slide-in" data-js-product style="--animation-order:{{ animation_order }}">
        <script class="data-json-product" type="application/json">{% render 'product-get-attributes', product: product, build_variant: 'json' %}</script>
        <div class="product-thumb js-product-images-navigation">
            {% render 'product-get-image', product: product, aspect_ratio: aspect_ratio %}
            {% render 'product-get-label', product: product %}
            {% render 'product-get-button-cart', product: product, popup: true %}
            <div class="product-btn popup">
                {% render 'product-get-button-wishlist', product: product %}
                {% render 'product-get-button-compare', product: product %}
                {% render 'product-get-button-quickview', product: product %}
            </div>
        </div>
        <div class="product-info skeleton-list">
            {% render 'product-get-title', product: product %}
            <div class="product-review">{{ settings.product_review_badge }}</div>
            {% render 'product-get-price', product: product %}
        </div>
        {% if list_content %}
            {% render 'product-style-list', product: product %}
        {% endif %}
    </div>
</div>